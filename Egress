ver=$(oc get clusterversion version -o jsonpath='{.status.desired.version}' 2>/dev/null)
if [[ $ver == 4.18* ]]; then
  echo "Cluster is on 4.18 (desired: $ver)"
  exit 0
else
  echo "Cluster is NOT 4.18 (desired: ${ver:-unknown})" >&2
  exit 1
fi
